/* Kinetic HR v0.7 — Decision Reality layer */
(()=>{
'use strict';
const VERSION='KH-DECISION-v0.7';
const q=(s,r=document)=>r.querySelector(s),qa=(s,r=document)=>[...r.querySelectorAll(s)];
const isAr=()=>document.documentElement.dir==='rtl';
const L=(ar,en)=>isAr()?ar:en;
const esc7=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const num=v=>{const n=Number(v);return Number.isFinite(n)?n:null};
const clamp=(v,a=0,b=1)=>Math.min(b,Math.max(a,v));
const money=v=>Number.isFinite(v)?new Intl.NumberFormat(isAr()?'ar-SA':'en-US',{maximumFractionDigits:0}).format(v)+' '+L('ر.س','SAR'):'—';
const DEMO_CONTEXT={
'demo-001':{funded_fte:50,utilization_rate:.99,overtime_hours_per_fte_month:18,sick_leave_rate:.064},
'demo-002':{funded_fte:80,utilization_rate:.93,overtime_hours_per_fte_month:10,sick_leave_rate:.041},
'demo-003':{funded_fte:71,utilization_rate:.88,overtime_hours_per_fte_month:6,sick_leave_rate:.032},
'demo-011':{funded_fte:50,utilization_rate:.78,overtime_hours_per_fte_month:2,sick_leave_rate:.021},
'demo-004':{funded_fte:108,utilization_rate:1.02,overtime_hours_per_fte_month:24,sick_leave_rate:.071},
'demo-005':{funded_fte:40,utilization_rate:1.04,overtime_hours_per_fte_month:27,sick_leave_rate:.079},
'demo-006':{funded_fte:65,utilization_rate:.82,overtime_hours_per_fte_month:5,sick_leave_rate:.028},
'demo-007':{funded_fte:86,utilization_rate:.98,overtime_hours_per_fte_month:19,sick_leave_rate:.061},
'demo-008':{funded_fte:35,utilization_rate:.94,overtime_hours_per_fte_month:13,sick_leave_rate:.046},
'demo-009':{funded_fte:74,utilization_rate:.80,overtime_hours_per_fte_month:4,sick_leave_rate:.025},
'demo-010':{funded_fte:24,utilization_rate:.96,overtime_hours_per_fte_month:15,sick_leave_rate:.052},
'demo-012':{funded_fte:18,utilization_rate:.81,overtime_hours_per_fte_month:3,sick_leave_rate:.024}
};
const state={safetyPct:.05,approval:{hire:0,transfer:0,upskill:0,contract:0},unitCost:{hire:null,transfer:null,upskill:null,contract:null},budget:null,uncoveredFteDayCost:null};
function ensureContext(){if(typeof SNAPSHOTS==='undefined')return;if(snapshotMode==='synthetic')SNAPSHOTS.forEach(r=>{const x=DEMO_CONTEXT[r.source_row];if(x)Object.entries(x).forEach(([k,v])=>{if(r[k]==null||r[k]==='')r[k]=v})})}
function fundingFor(r){const need=requiredFte(r),avail=availableFte(r),g=deficit(r),raw=num(r.funded_fte);if(raw==null)return{known:false,fundedGap:null,unfundedGap:null,fundedNeed:null,status:'unknown'};const fundedNeed=clamp(raw,0,need),fundedGap=Math.max(0,Math.min(need,fundedNeed)-avail),unfundedGap=Math.max(0,g-fundedGap);return{known:true,fundedGap,unfundedGap,fundedNeed,status:unfundedGap>0&&fundedGap>0?'mixed':unfundedGap>0?'unfunded':g>0?'funded':'covered'}}
function pressureFor(r){const u=num(r.utilization_rate),o=num(r.overtime_hours_per_fte_month),s=num(r.sick_leave_rate);if([u,o,s].filter(Number.isFinite).length<2)return{known:false,score:null,band:'unknown'};let w=0,total=0;if(Number.isFinite(u)){total+=.5*clamp((u-.80)/.20);w+=.5}if(Number.isFinite(o)){total+=.3*clamp(o/25);w+=.3}if(Number.isFinite(s)){total+=.2*clamp(s/.08);w+=.2}const score=Math.round(100*(w?total/w:0)),band=score>=70?'high':score>=45?'medium':'low';return{known:true,score,band,inputs:{utilization:u,overtime:o,sickLeave:s}}}
KHPlain.pressure=pressureFor;
function transferableSurplus(donor,pct=state.safetyPct){if(!donor)return 0;const reserve=Math.max(0,requiredFte(donor)*clamp(Number(pct)||0,0,.5));return Math.max(0,surplus(donor)-reserve)}
function rebalanceOptions(r){return SNAPSHOTS.filter(d=>d.sector===r.sector&&String(d.ssco)===String(r.ssco)&&cellCode(d)!==cellCode(r)&&surplus(d)>0).map(d=>({row:d,raw:surplus(d),transferable:transferableSurplus(d),exactLevel:d.level===r.level,exactCategory:d.category===r.category})).filter(x=>x.transferable>0).sort((a,b)=>(Number(b.exactLevel)+Number(b.exactCategory))-(Number(a.exactLevel)+Number(a.exactCategory))||b.transferable-a.transferable)}
function executiveTranslation(r){return r?KHPlain.summary(r,fundingFor(r),pressureFor(r)):KHPlain.summary(null)}
function aggregateFunding(rows=currentRows()){let funded=0,unfunded=0,unknown=0;rows.forEach(r=>{const f=fundingFor(r);if(!f.known){unknown+=deficit(r);return}funded+=f.fundedGap;unfunded+=f.unfundedGap});return{funded,unfunded,unknown}}
function parseInput(id){const el=q('#'+id);if(!el||String(el.value).trim()==='')return null;const n=Number(el.value);return Number.isFinite(n)?Math.max(0,n):null}
function readRealityInputs(){state.safetyPct=clamp((parseInput('v07-safety-pct')??5)/100,0,.5);['hire','transfer','upskill','contract'].forEach(k=>{state.approval[k]=parseInput('v07-approval-'+k)||0;state.unitCost[k]=parseInput('v07-cost-'+k)});state.budget=parseInput('v07-budget');state.uncoveredFteDayCost=parseInput('v07-gapday-cost')}
const baseScenarioValues=typeof scenarioValues==='function'?scenarioValues:null;
if(baseScenarioValues)scenarioValues=function(){const v=baseScenarioValues();readRealityInputs();return{...v,safetyBufferPct:state.safetyPct,approvalLatencyDays:{...state.approval},unitCostSar:{...state.unitCost},budgetSar:state.budget,uncoveredFteDayCostSar:state.uncoveredFteDayCost,decisionLayer:VERSION}};
const baseEffect=typeof interventionEffectAtDay==='function'?interventionEffectAtDay:null;
if(baseEffect)interventionEffectAtDay=function(kind,units,day,r,opts={}){const lag=Math.max(0,Number(state.approval[kind]||0));if(day<lag)return 0;return baseEffect(kind,units,day-lag,r,opts)};
const baseScenarioImpact=typeof scenarioImpact==='function'?scenarioImpact:null;
if(baseScenarioImpact)scenarioImpact=function(r,vals){const donor=vals.donorCell?getRowByCell(vals.donorCell):null,cap=donor?transferableSurplus(donor,vals.safetyBufferPct??state.safetyPct):0,requested=Math.max(0,Number(vals.transfer||0)),actual=Math.min(requested,cap),adjusted={...vals,transfer:actual};const out=baseScenarioImpact(r,adjusted),costs=vals.unitCostSar||state.unitCost;let totalCost=0,costKnown=true;for(const k of ['hire','transfer','upskill','contract']){const units=k==='transfer'?actual:Math.max(0,Number(vals[k]||0));if(!units)continue;const c=num(costs?.[k]);if(c==null){costKnown=false;continue}totalCost+=units*c}const horizon=Math.max(30,Number(vals.horizonDays||180));let recipientGapDays=0,donorExtraGapDays=0;for(let day=0;day<horizon;day++){const effect=['hire','upskill','contract','transfer'].reduce((a,k)=>a+interventionEffectAtDay(k,k==='transfer'?actual:vals[k],day,r),0);recipientGapDays+=Math.max(0,requiredFte(r)-(availableFte(r)+effect));if(donor&&actual>0){const tr=interventionEffectAtDay('transfer',actual,day,r);donorExtraGapDays+=Math.max(0,requiredFte(donor)-(availableFte(donor)-tr))-deficit(donor)}}const baseGapDays=deficit(r)*horizon,gapDaysAvoided=Math.max(0,baseGapDays-recipientGapDays-Math.max(0,donorExtraGapDays)),daily=num(vals.uncoveredFteDayCostSar),financialAvoided=daily==null?null:gapDaysAvoided*daily,budget=num(vals.budgetSar),budgetState=!costKnown?'unknown':budget==null?'not_set':totalCost<=budget?'within':'over';const warnings=[...(out.warnings||[])];if(requested>actual)warnings.push(L(`تم تقييد النقل من ${fmt(requested,1)} إلى ${fmt(actual,1)} ${KHPlain.unit()} بعد staffing reserve.`,`Transfer capped from ${fmt(requested,1)} to ${fmt(actual,1)} ${KHPlain.unit()} after the safety buffer.`));return{...out,requestedTransfer:requested,actualTransfer:actual,transferableSurplus:cap,totalCostSar:costKnown?totalCost:null,costKnown,budgetSar:budget,budgetState,gapDaysAvoided,financialExposureAvoidedSar:financialAvoided,costPerBurdenUnit:costKnown&&out.burdenReduction>0?totalCost/out.burdenReduction:null,approvalLatencyDays:{...(vals.approvalLatencyDays||state.approval)},decisionLayer:VERSION,warnings}};
const baseSave=typeof saveScenarioRecord==='function'?saveScenarioRecord:null;
if(baseSave)saveScenarioRecord=function(r,vals){const rec=baseSave(r,vals);try{const rows=readScenarioLedger(),i=rows.findIndex(x=>x.id===rec.id);if(i>=0){rows[i].decision_layer=VERSION;rows[i].inputs={...vals};rows[i].impact=scenarioImpact(r,vals);writeScenarioLedger(rows)}appendAuditRecord({key:'decision_v07|'+rec.id,kind:'decision_scenario',scenario_id:rec.id,cell:cellCode(r),detail:`${VERSION}; budget=${vals.budgetSar??'NA'}; safety=${Math.round((vals.safetyBufferPct??.05)*100)}%`,basis:VERSION,source:r.source_row,fingerprint:stableStateFingerprint()})}catch{}return rec};
function injectShell(){if(q('#v07-exec-translation'))return;const top=q('.top-actions');if(top){const b=document.createElement('button');b.id='v07-brief-btn';b.className='utility-btn v07-brief-btn';b.type='button';b.textContent=L('ملخص تنفيذي','Executive Brief');top.insertBefore(b,top.firstChild);b.onclick=openBrief}const metrics=q('#executive-metrics');if(metrics){const box=document.createElement('article');box.id='v07-exec-translation';box.className='v07-exec-translation';metrics.insertAdjacentElement('afterend',box)}const controls=q('.scenario-controls');if(controls){const panel=document.createElement('details');panel.id='v07-reality-panel';panel.className='v07-reality-panel';panel.open=true;panel.innerHTML=realityPanelHtml();const anchor=q('#scenario-baseline',controls);anchor?anchor.insertAdjacentElement('afterend',panel):controls.appendChild(panel);panel.addEventListener('input',()=>{readRealityInputs();if(typeof renderScenario==='function')renderScenario()});panel.addEventListener('change',()=>{readRealityInputs();if(typeof renderScenario==='function')renderScenario()})}const result=q('.scenario-result');if(result){const div=document.createElement('div');div.id='v07-decision-result';div.className='v07-decision-result';result.appendChild(div)}const overlay=document.createElement('div');overlay.id='v07-brief-overlay';overlay.className='v07-brief-overlay';overlay.innerHTML='<div class="v07-brief-sheet"><div id="v07-brief-content"></div><div class="v07-brief-actions"><button id="v07-print-brief" class="primary-btn">'+L('طباعة / حفظ PDF','Print / Save PDF')+'</button><button id="v07-close-brief" class="secondary-btn">'+L('إغلاق','Close')+'</button></div></div>';document.body.appendChild(overlay);q('#v07-close-brief').onclick=()=>overlay.classList.remove('open');q('#v07-print-brief').onclick=()=>window.print();enhanceReviewer()}
function realityPanelHtml(){return `<summary>${L('واقعية القرار: التمويل · الموافقات · التكلفة · إعادة التوازن','Decision reality: funding · approvals · cost · rebalancing')}</summary><div class="v07-reality-body"><div class="v07-input-grid"><label><span>${L('هامش الأمان للجهة المانحة %','Donor safety buffer %')}</span><input id="v07-safety-pct" type="number" min="0" max="50" value="5"></label><label><span>${L('الميزانية المتاحة للسيناريو (ر.س)','Scenario budget (SAR)')}</span><input id="v07-budget" type="number" min="0" placeholder="${L('أدخل قيمة موثقة','Enter verified value')}"></label><label><span>${L('تكلفة يوم واحد من النقص بدوام كامل (ر.س)','Cost per uncovered FTE-day (SAR)')}</span><input id="v07-gapday-cost" type="number" min="0" placeholder="${L('اختياري · من المالية','Optional · Finance source')}"></label></div><div class="v07-cost-grid">${['hire','transfer','upskill','contract'].map(k=>`<article><strong>${{hire:L('توظيف','Hire'),transfer:L('نقل','Transfer'),upskill:L('تأهيل','Upskill'),contract:L('تعاقد','Contract')}[k]}</strong><label><span>${L('مهلة الموافقة (يوم)','Approval time (days)')}</span><input id="v07-approval-${k}" type="number" min="0" value="0"></label><label><span>${L('تكلفة الوحدة (ر.س)','Unit cost (SAR)')}</span><input id="v07-cost-${k}" type="number" min="0" placeholder="${L('غير مفترضة','Not assumed')}"></label></article>`).join('')}</div><p class="v07-guardrail">${L('العرض التجريبي يستخدم تكاليف ومهل موافقة توضيحية قابلة للتعديل. للبيانات الفعلية، تحقّق من القيم مع الموارد البشرية والمالية قبل اعتماد القرار.','The demo uses editable, illustrative costs and approval delays. For actual entity data, verify values with HR and Finance before approving a decision.')}</p></div>`}
function selectedRow(){return typeof selectedScenarioSource!=='undefined'?getRowBySource(selectedScenarioSource):null}
function renderDecisionLayer(){ensureContext();const engine=q('#sidebar-engine-state');if(engine)engine.textContent=`${MEASUREMENT_VERSION} · ${VERSION} · REVIEW-v0.6`;renderExecutive();augmentGaps();renderReality();enhanceReviewer()}
function renderExecutive(){const rows=[...currentRows()].filter(r=>deficit(r)>0).sort((a,b)=>(decisionPriority(b).score??-1)-(decisionPriority(a).score??-1)),r=rows[0],box=q('#v07-exec-translation');if(!box)return;const af=aggregateFunding();box.innerHTML=`<div class="v07-exec-head"><div><span>${L('ملخص القرار','DECISION SUMMARY')}</span><strong>${L('أين النقص، وما الخطوة التالية؟','Where is the shortfall, and what comes next?')}</strong></div><button class="v07-inline-brief">${L('ملخص صفحة واحدة','One-page brief')}</button></div><p>${esc7(executiveTranslation(r)||L('لا توجد فجوة نشطة في القطاع المحدد.','No active gap in the selected sector.'))}</p><div class="v07-funding-strip"><span><small>${L('نقص بتمويل متاح','Shortfall with funding')}</small><b>${fmt(af.funded,1)} ${KHPlain.unit()}</b></span><span><small>${L('نقص يحتاج تمويلًا','Shortfall needing funding')}</small><b>${fmt(af.unfunded,1)} ${KHPlain.unit()}</b></span><span><small>${L('تمويل غير معروف','Funding unknown')}</small><b>${fmt(af.unknown,1)} ${KHPlain.unit()}</b></span></div>`;q('.v07-inline-brief',box).onclick=openBrief}
function augmentGaps(){qa('#gaps-body tr').forEach(tr=>{if(q('.v07-context-badges',tr))return;const btn=q('[data-detail]',tr);if(!btn)return;const r=getRowBySource(btn.dataset.detail);if(!r)return;const f=fundingFor(r),p=pressureFor(r),cell=q('.occupation-cell',tr);if(!cell)return;const wrap=document.createElement('div');wrap.className='v07-context-badges';wrap.innerHTML=`<span class="fund ${f.status}">${f.known?(f.status==='funded'?L('ممولة','Funded'):f.status==='unfunded'?L('غير ممولة','Unfunded'):f.status==='mixed'?L('تمويل مختلط','Mixed funding'):L('مغطاة','Covered')):L('التمويل غير معروف','Funding unknown')}</span>${p.known?`<span class="pressure ${p.band}">${L('ضغط الفريق','Team pressure')} ${p.score}</span>`:''}`;cell.appendChild(wrap)})}
function renderReality(){const r=selectedRow(),out=q('#v07-decision-result');if(!r||!out)return;readRealityInputs();const vals=scenarioValues(),impact=scenarioImpact(r,vals),fund=fundingFor(r),pressure=pressureFor(r),opts=rebalanceOptions(r),best=opts[0],budgetText=impact.budgetState==='within'?L('ضمن الميزانية','Within budget'):impact.budgetState==='over'?L('يتجاوز الميزانية','Over budget'):impact.budgetState==='not_set'?L('لم تُدخل ميزانية','Budget not entered'):L('التكلفة غير مكتملة','Cost inputs incomplete');out.innerHTML=`<div class="v07-decision-grid"><article><span>${L('التمويل','Funding')}</span><strong>${fund.known?`${fmt(fund.fundedGap,1)} ${L('ممولة','funded')} · ${fmt(fund.unfundedGap,1)} ${L('غير ممولة','unfunded')}`:'—'}</strong><small>${L('يفصل الحاجة التشغيلية عن القدرة المعتمدة ماليًا.','Separates operational need from funded capacity.')}</small></article><article><span>${L('ضغط الفريق','Team pressure')}</span><strong>${pressure.known?`${pressure.score}/100 · ${isAr()?({high:'مرتفع',medium:'متوسط',low:'منخفض'}[pressure.band]||pressure.band):pressure.band}`:'—'}</strong><small>${L('مؤشر تشغيلي سياقي، لا يشخّص الاحتراق الوظيفي ولا يغيّر الأولوية تلقائيًا.','Contextual operating-pressure proxy; not a burnout diagnosis and not an automatic priority modifier.')}</small></article><article><span>${L('تكلفة السيناريو','Scenario cost')}</span><strong>${impact.costKnown?money(impact.totalCostSar):'—'}</strong><small>${budgetText}</small></article><article><span>${L('التكلفة مقابل الأثر','Cost-to-Impact')}</span><strong>${Number.isFinite(impact.costPerBurdenUnit)?money(impact.costPerBurdenUnit):'—'}</strong><small>${L('ر.س لكل وحدة من العبء التشغيلي المتجنب.','SAR per avoided operational-burden unit.')}</small></article></div><div class="v07-rebalance"><div><span>${L('إعادة التوازن الداخلي','INTERNAL REBALANCING')}</span><strong>${best?L(`أفضل مجموعة وظائف يمكن النقل منها متاحة: ${sectorCfg(best.row.sector).locations.find(x=>x.id===best.row.location)?.ar||best.row.location}`,`Best available source job group: ${sectorCfg(best.row.sector).locations.find(x=>x.id===best.row.location)?.en||best.row.location}`):L('لا توجد مجموعة وظائف يمكن النقل منها آمنة لنفس SSCO','No safe source job group for the same SSCO')}</strong>${best?`<small>${L('فائض قبل حجز الاحتياطي','Surplus before the reserve')}: ${fmt(best.raw,1)} ${KHPlain.unit()} · ${L('قابل للنقل بعد هامش الأمان','Transferable after safety buffer')}: ${fmt(best.transferable,1)} ${KHPlain.unit()} · ${L('مطابقة المستوى/الفئة','Level/category match')}: ${best.exactLevel&&best.exactCategory?'✓':'△'}</small>`:''}</div>${best?`<button id="v07-apply-rebalance" class="secondary-btn">${L('جرّب إعادة التوازن','Test rebalancing')}</button>`:''}</div><div class="v07-feasibility"><b>${L('بوابة الجدوى','Feasibility gate')}:</b> ${L('SSCO مطابق','SSCO match')} ✓ · ${L('هامش الأمان','Safety buffer')} ✓ · ${L('المهارات/أهلية الأفراد','Skills/person eligibility')} ? · ${L('موافقة الموارد البشرية والمالية','HR/Finance approval')} ?</div>${impact.financialExposureAvoidedSar!=null?`<div class="v07-financial"><span>${L('تكلفة نقص متوقع تجنبها','Estimated shortfall cost avoided')}</span><strong>${money(impact.financialExposureAvoidedSar)}</strong><small>${L('يعتمد فقط على تكلفة يوم واحد من النقص بدوام كامل التي أدخلتها الجهة؛ ليس رقمًا مولدًا من النظام.','Uses only the entity-entered cost per uncovered FTE-day; it is not system-invented.')}</small></div>`:''}`;const b=q('#v07-apply-rebalance');if(b&&best)b.onclick=()=>{const donor=q('#scenario-donor'),range=q('#transfer-range');if(donor){donor.value=cellCode(best.row);donor.dispatchEvent(new Event('change',{bubbles:true}))}if(range){range.value=Math.min(Number(range.max||30),Math.ceil(Math.min(deficit(r),best.transferable)));range.dispatchEvent(new Event('input',{bubbles:true}))}}}
function briefRows(){return [...currentRows()].filter(r=>deficit(r)>0).sort((a,b)=>(decisionPriority(b).score??-1)-(decisionPriority(a).score??-1)).slice(0,5)}
function openBrief(){ensureContext();const rows=briefRows(),sum=sectorSummary(),af=aggregateFunding(),fp=stableStateFingerprint();q('#v07-brief-content').innerHTML=`<header class="v07-brief-header"><div><span>KINETIC HR</span><h1>${L('موجز تنفيذي للقوى العاملة','Workforce Executive Brief')}</h1><p>${CONFIG.sectors[activeSector]?.[isAr()?'ar':'en']||activeSector} · ${isoDate(getAsOfDate())}</p></div><div><small>${L('التغطية المطابقة','Matched coverage')}</small><strong>${fmt(sum.coverage*100,1)}%</strong></div></header><section class="v07-brief-summary"><div><span>${L('الفجوة الحالية','Current gap')}</span><b>${fmt(sum.gap,1)} ${KHPlain.unit()}</b></div><div><span>${L('نقص جديد خلال 30 يومًا','New shortfall in 30 days')}</span><b>${KHPlain.n(KHPlain.newShortfall().value)}</b></div><div><span>${L('نقص بتمويل متاح','Shortfall with funding')}</span><b>${fmt(af.funded,1)}</b></div><div><span>${L('نقص يحتاج تمويلًا','Shortfall needing funding')}</span><b>${fmt(af.unfunded,1)}</b></div></section><p class="kh-unit-help">${KHPlain.unitHelp()} ${KHPlain.newShortfall().complete?'':L('النقص الجديد يشمل فقط المجموعات ذات السجل المكتمل.','New shortfall includes only groups with complete history.')}</p><section><h2>${L('ما يحتاج قرارًا الآن','What needs a decision now')}</h2>${rows.map((r,i)=>{const f=fundingFor(r),p=pressureFor(r),opt=rebalanceOptions(r)[0],loc=sectorCfg(r.sector).locations.find(x=>x.id===r.location)?.[isAr()?'ar':'en']||r.location;return `<article class="v07-brief-item"><b>${i+1}</b><div><strong>${esc7(occupationLabel(r.ssco))} · ${esc7(loc)}</strong><p>${esc7(executiveTranslation(r))}</p>${KHPlain.detail(r,f,p)}</div></article>`}).join('')}</section><footer><p>${L('بيانات العرض تجريبية. راجع افتراضات التكلفة مع المالية قبل التنفيذ. رمز التحقق SHA-256 أدناه يساعد على معرفة ما إذا كانت البيانات تغيّرت، لكنه لا يمنع تعديلها.','Demo data are synthetic. Review cost assumptions with Finance before execution. The SHA-256 verification code below helps detect changes to the data; it does not prevent changes.')}</p><code>${fp}</code></footer>`;q('#v07-brief-overlay').classList.add('open')}
function enhanceReviewer(){const panel=q('#kh-review-overlay .kh-review-panel');if(panel&&!q('#v07-review-context',panel)){const intro=document.createElement('div');intro.id='v07-review-context';intro.className='v07-review-context';intro.innerHTML=`<strong>${L('كيف تراجع الأرقام دون معرفة تقنية؟','How to review the numbers without technical knowledge')}</strong><span>${L('ابدأ من الأدلة: اختر خلية، راجع المطلوب والمتاح، ثم الأحداث المؤرخة التي أنشأت حالة الفجوة، ثم قارن المعدل الحالي بخط الأساس. بعدها افحص التمويل وضغط الفريق وقيود السيناريو.','Start with Evidence: choose a cell, verify Required/Available, inspect dated events that created the Gap Episode, then compare current rate with baseline. Then review funding, team pressure and scenario constraints.')}</span>`;q('.kh-review-body',panel)?.insertAdjacentElement('beforebegin',intro);const pilot=q('[data-rpage-panel="pilot"]',panel);if(pilot&&!q('.v07-pilot-ownership',pilot)){const c=document.createElement('div');c.className='kh-limitations v07-pilot-ownership';c.innerHTML=`<strong>${L('حوكمة التجربة المؤسسية المقترحة','Recommended pilot governance')}</strong><br>${L('ملكية مشتركة: تخطيط القوى العاملة الاستراتيجي والمالية، مع راعٍ تشغيلي من القطاع محل التجربة. الرسالة الداخلية: أداة تمكين لتسريع الموافقة والميزانية، وليست أداة رقابة على المديرين.','Joint ownership: Strategic Workforce Planning + Finance, with an operational sponsor from the pilot sector. Internal positioning: a tool to accelerate evidence-based approvals and funding, not a surveillance tool for managers.')}`;pilot.prepend(c)}}}
const baseRenderAll=typeof renderAll==='function'?renderAll:null;if(baseRenderAll)renderAll=function(){ensureContext();baseRenderAll();queueMicrotask(renderDecisionLayer)};
const baseRenderScenario=typeof renderScenario==='function'?renderScenario:null;if(baseRenderScenario)renderScenario=function(){ensureContext();baseRenderScenario();queueMicrotask(renderReality)};
const baseReset=typeof resetDemoData==='function'?resetDemoData:null;if(baseReset)resetDemoData=function(){baseReset();ensureContext();queueMicrotask(renderDecisionLayer)};
document.addEventListener('kinetic:languagechange',()=>{
  const panel=q('#v07-reality-panel');
  if(panel){const values=Object.fromEntries(qa('input,select',panel).map(el=>[el.id,{value:el.value,edited:el.dataset.userEdited}]));panel.innerHTML=realityPanelHtml();for(const [id,field] of Object.entries(values)){const el=q('#'+id,panel);if(el){el.value=field.value;el.dataset.userEdited=field.edited||'0'}}}
  q('#v07-review-context')?.remove();q('.v07-pilot-ownership')?.remove();enhanceReviewer();
  q('#v07-close-brief').textContent=L('إغلاق','Close');q('#v07-print-brief').textContent=L('طباعة / حفظ PDF','Print / Save PDF');
  if(q('#v07-brief-overlay')?.classList.contains('open'))openBrief();
});
document.addEventListener('DOMContentLoaded',()=>{ensureContext();injectShell();readRealityInputs();setTimeout(renderDecisionLayer,0)});
})();